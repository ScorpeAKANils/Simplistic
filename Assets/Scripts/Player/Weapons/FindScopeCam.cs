using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FindScopeCam : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
       //var scopeCam = FindObjectOfType<SetRenderTexture>();
       //scopeCam.transform.position = this.transform.position;
       //scopeCam.transform.parent = this.transform; 
    }
}
